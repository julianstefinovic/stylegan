#entrypoint: python runway_model.py
version: 0.1
python: 3.6
framework: tensorflow
cuda: 9.0
spec:
  gpu: True
  cpu: False
files:
  ignore:
    - checkpoints/*
build_steps:
  - pip install -r requirements.txt
